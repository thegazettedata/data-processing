#!/bin/bash
PROJECT_NAME="school-voting"

# School salary spreadsheets that we received
# ic = Iowa City
# cr = Cedar Rapids
# ic-names = Iowa City while runng deduping with only names and not addresses
EMPLOYEES=("cr")

# The counties for the voter data
# joco = Johnson County
# linn = Linn County
TOPICS=("joco" "linn")